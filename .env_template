POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_password
POSTGRES_DB=wallet_db

# `db` — имя сервиса из docker-compose.yml, 5432 — порт внутри docker-сети.
POSTGRES_HOST=db
POSTGRES_PORT=5432
